{% extends 'base.html.twig' %}

{% block title %}Hike{% endblock %}

{% block body %}
    <h1>Hike</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Difficultée</th>
                <td>
                    {% if hike.difficulty.level is defined %}
                    {{ hike.difficulty.level }}
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hike.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ hike.Content }}</td>
            </tr>
            <tr>
                <th>Length</th>
                <td>{{ hike.length }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ hike.duration }}</td>
            </tr>
            <tr>
                <th>Elevation_gain</th>
                <td>{{ hike.elevationGain }}</td>
            </tr>
            <tr>
                <th>Elevation_loss</th>
                <td>{{ hike.elevationLoss }}</td>
            </tr>
            <tr>
                <th>Gps_coordonate</th>
                <td>{{ hike.gpsCoordonate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hike_index') }}">back to list</a>

    <a href="{{ path('app_hike_edit', {'id': hike.id}) }}">edit</a>

    {{ include('hike/_delete_form.html.twig') }}
{% endblock %}
