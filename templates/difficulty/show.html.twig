{% extends 'base.html.twig' %}

{% block title %}Difficulty{% endblock %}

{% block body %}
    <h1>Difficulty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ difficulty.id }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ difficulty.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_difficulty_index') }}">back to list</a>

    <a href="{{ path('app_difficulty_edit', {'id': difficulty.id}) }}">edit</a>

    {{ include('difficulty/_delete_form.html.twig') }}
{% endblock %}
